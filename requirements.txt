# Job Agent — Python dependencies
# Install: pip3 install -r requirements.txt --break-system-packages

anthropic>=0.105.0
ddgs>=9.0.0
python-dotenv>=1.0.0
gspread>=6.0.0
google-auth>=2.0.0
google-api-python-client>=2.0.0
beautifulsoup4>=4.12.0
requests>=2.31.0

# Business Opportunity Finder (biz_agent.py)
# Uses the same anthropic + beautifulsoup4 + requests — no extra installs needed
